server:
  port: ${EUREKA_SERVER_PORT:8761} # Port mặc định và là quy chuẩn quốc tế của Eureka Server

eureka:
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}
  client:
    register-with-eureka: false
    fetch-registry: false